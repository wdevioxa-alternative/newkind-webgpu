export default (self) => {
    return new Promise(async (resolve, reject) => {

        // if(self.slot === 'footer_relation' || self.slot === 'footer_property') {
            // let div = document.createElement('div')
            // div.classList.add('footer-time')
            // div.insertAdjacentHTML('afterbegin', `<slot name="${self.slot}_time"></slot>`)
            // self.attachShadow({mode: "open"});
            // self.shadowRoot.appendChild(div)
            // self.insertAdjacentHTML('afterbegin', `
            //     <api-time slot="${self.slot}_time">
            //         <p class="timer">Relation</p>
            //     </api-time>
            // `)
        // }

        // if(self.slot === 'footer_substrate') {
            // const p1 = document.createElement('p')
            // const p2 = document.createElement('p')
            // const p3 = document.createElement('p')
            // p1.classList.add('grid__footer_substrate_title')
            // p2.classList.add('grid__footer_substrate_mail')
            // p3.classList.add('grid__footer_substrate_tel')
            // p1.textContent = 'Zababurin Sergey'
            // p2.insertAdjacentHTML('afterbegin', `
            //     Email: <a href="mailto:zababurins@vk.com">zababurins@vk.com</a>
            // `)
            // p3.insertAdjacentHTML('afterbegin', `
            //     Phone: <a href="tel:+79531437072">+7 953-143-70-72</a>
            // `)
            // self.shadowRoot.appendChild(p1)
            // self.shadowRoot.appendChild(p2)
            // self.shadowRoot.appendChild(p3)
        // }

        resolve({


        })
    })
}
