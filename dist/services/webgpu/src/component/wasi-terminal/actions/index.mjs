export const actions = (self) => {
    return new Promise(async (resolve, reject) => {

        resolve({
            click: async (event) => {

            }
        });
    });
};

export default {
    description: 'action'
};